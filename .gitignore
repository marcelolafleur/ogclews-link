exp01_out/
ogclews_runs/
figs/
__pycache__/
*.pyc
*.pkl
*.png

# Python build artifacts (uv build / pip wheel)
build/
dist/
*.egg-info/

# uv standalone environment (uv.lock IS committed; the venv is not)
.venv/

# Local-only data export + scratch — never commit
IHME-GBD_2023_DATA/
.review_tmp/

# Test-battery per-run progress (the golden baseline results/golden.json IS committed; this is not)
results/battery-state.json

# per-machine installed OG-model register (written by the installer / models register)
og_model_registry.json

# macOS finder junk
.DS_Store

# Publisher PDFs read during the IEEM / CLEWs-IO assessments. Licensed copies,
# not ours to redistribute -- keep them local, never commit.
*.pdf
