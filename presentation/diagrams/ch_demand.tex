% Channel flow (plain language) — DEMAND: a growing economy needs more energy services,
% so the energy system builds more -- raising cost & emissions that loop back. Only bites
% once the two models run together.
\documentclass[border=14pt]{standalone}
\input{_preamble}
\begin{document}
\begin{tikzpicture}

\draw[claret, line width=2.2pt] (-0.55,2.55) -- (0.15,2.55);
\node[flowtitle, anchor=west] at (-0.6,2.2) {Demand — a growing economy needs energy};

\stage{s1}{0}{0}{ogc}{\faChartLine}{The economy and incomes grow}
\stage{s2}{3.8}{0}{ogc}{\faUsers}{Homes, transport \& industry do more}
\stage{s3}{7.6}{0}{amber}{\faLightbulb}{More energy services needed}
\stage{s4}{11.4}{0}{clews}{\faIndustry}{The energy system builds more}
\node[stagecircle, fill=slate] (econ) at (15.0,0) {\faSync};
\node[font=\small, text=ink, anchor=west, text width=2.7cm] at (16.0,0) {Back to the economy, via the loop};

\draw[flowedge] (s1) -- (s2);
\draw[flowedge] (s2) -- (s3);
\draw[flowedge] (s3) -- (s4);
\draw[flowedge, slate, dashed] (s4) -- (econ);

% ---- caption ----------------------------------------------------------------
\node[flowcap, text width=16cm] at (7.5,-2.95)
  {A growing economy needs more energy services, so the system builds more capacity — raising costs
   and emissions that loop back to the economy. This link only takes effect once the two models are
   run back and forth together.};

\end{tikzpicture}
\end{document}
