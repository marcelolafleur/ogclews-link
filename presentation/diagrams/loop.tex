% Diagram 3 — the iterated soft-link: the orchestrator drives a damped quantity/price
% exchange to a fixed point (the MESSAGE-MACRO / TIMES-MACRO pattern). Honest about what
% runs today (one CLEWS->OG pass) vs the external CLEWS re-solve seam (stubbed).
\documentclass[border=12pt]{standalone}
\input{_preamble}

\tikzset{
  modelnode/.style={panel, text width=4.6cm, align=center, minimum height=1.55cm,
                    font=\footnotesize},
  ognode/.style={modelnode, draw=ogc, fill=ogc!5},
  clewsnode/.style={modelnode, draw=clews, fill=clews!5},
  transfer/.style={rounded corners=4pt, line width=0.9pt, inner sep=7pt, align=center,
                   text width=3.9cm, font=\scriptsize},
  hub/.style={circle, draw=ink, line width=1.1pt, fill=grid!40, align=center,
              font=\scriptsize\bfseries, text=ink, inner sep=4pt, minimum size=2.1cm},
  anchor box/.style={card, draw=mute, fill=grid!25, text width=3.7cm, align=center,
                     font=\scriptsize, text=sub},
  ring/.style={line width=1.6pt, -{Stealth[length=3mm,width=2.5mm]}},
  flag/.style={rounded corners=2.5pt, draw=policy, fill=policy!8, text=policy,
               font=\scriptsize\bfseries, inner xsep=5pt, inner ysep=3pt, align=center},
}

\begin{document}
\begin{tikzpicture}

% ---- the four stations + hub ------------------------------------------------
\node[ognode]    (og)    at (0, 2.7)  {\textbf{\color{ogc}Solve the economy}\\[1pt]
                                       {\scriptsize\color{sub}OG-Core}};
\node[clewsnode] (clews) at (0,-2.7)  {\textbf{\color{clews}Solve the energy system}\\[1pt]
                                       {\scriptsize\color{sub}CLEWS / OSeMOSYS}};
\node[transfer, draw=ogc!55, fill=ogc!6, text=ink]   (emit)  at (6.6, 0)
     {\textbf{the economy $\rightarrow$ the system}\\[1pt]{\color{sub}energy demand · interest rate}};
\node[transfer, draw=clews!55, fill=clews!6, text=ink] (apply) at (-6.6,0)
     {\textbf{the system $\rightarrow$ the economy}\\[1pt]{\color{sub}energy price · build cost · emissions}};
\node[hub] (hub) at (0,0) {ogclews-\\link};

% ---- the clockwise ring (emit->CLEWS dashed: the external re-solve seam) -----
\draw[ring, ogc]          (og.east)    to[out=-35,in=110]  (emit.north);
\draw[ring, ogc, dashed]  (emit.south) to[out=-110,in=35]  (clews.east);
\draw[ring, clews]        (clews.west) to[out=145,in=-110] (apply.south);
\draw[ring, clews]        (apply.north) to[out=70,in=215]  (og.west);

% ---- hub caption ------------------------------------------------------------
\node[font=\scriptsize, text=sub, align=center, below=0.15cm of hub]
  {\textbf{\color{ink}the coupler}\\ runs the loop};

% ---- baseline anchor feeding the loop ---------------------------------------
\node[anchor box] (base) at (-6.6, 3.2)
  {\textbf{\color{ink}Start from baseline}\\ both models match their own baselines first};
\draw[thinflow, mute] (base.east) to[out=0,in=150] (og.north west);

% ---- outputs leaving the loop -----------------------------------------------
\node[anchor box, draw=ink!30, fill=white, text=ink] (out) at (8.7, -3.4)
  {\textbf{Results}\\ growth · who's affected · public finances};
\draw[thinflow, ink!55] (clews.south east) to[out=-25,in=170] (out.west);

% ---- convergence condition (on the apply -> OG return) ----------------------
\node[note, text=sub, align=center, anchor=east] at (-3.3, 1.65)
  {\footnotesize\itshape converged when\\ energy demand\\ stops changing};

% ---- honesty flag on the dashed re-solve seam -------------------------------
\node[flag, align=center] (stub) at (9.0,-0.95)
  {not built yet\\[1pt]{\scriptsize\mdseries the automatic re-solve}};

% (the "validated today: one pass" note lives in the deck caption, not here)

\end{tikzpicture}
\end{document}
