% TODO(abstract): ~150-200 words. Cover, in order:
%  - the gap: energy-system optimizers (CLEWS/OSeMOSYS) have no behaviour, fiscal
%    sector, or distribution; macro models have no energy technology detail.
%  - the contribution: a channel-based *soft* link between OG-Core (an OLG general-
%    equilibrium model) and CLEWS, each channel an auditable economic mechanism.
%  - what is distinctive: heterogeneous incidence (J income x S age), fiscal closure,
%    and a demographic health channel (emissions -> mortality/morbidity -> labour).
%  - validation: a regression-locked, cross-environment battery on an M=8 OG-PHL
%    calibration coupled to a real CLEWS scenario (coupled run); the PHL LCOE rises ~5%,
%    health carries the transition gains, the price channels the steady-state
%    contraction; incidence is regressive; signs are economically sound.
%  - honest scope: the CLEWS->OG pass AND the CLEWS re-solve seam validated (patch a case
%    copy, re-solve in the energy model's env, re-read; +4.3% production for +10% demand on
%    PHL v9); the outer iteration controller and unit calibration remain open.
The integration couples \OGCore{} and \CLEWS{} through a small set of economically
grounded channels. \emph{Abstract to be written last.}
