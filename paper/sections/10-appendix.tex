\section{Notation}
\label{app:notation}
% TODO: glossary table of symbols -- S, J, M, I; tau^c, tau^b, alpha_I, alpha_T;
% K, K_g, gamma_m, gamma_{g,m}, Z; r, r_p, w, p_m; rho(s,t), e(t,s,j), c_min; theta_m.

\section{OG-Core equations}
\label{app:og-equations}
% TODO: the household problem, firm FOCs, government budget, and market clearing in
% full -- the detail compressed in Sec. 3.1.

\section{The full channel-to-variable map}
\label{app:channel-map}
% TODO: the appendix-level diagram (reuse presentation diagram `channels`): each of the
% eight channels at the variable level, with direction (colour + arrowhead) and theory
% status (line style). NB the deck diagram currently shows seven -- add energy_capex.

\section{Battery items and full golden tables}
\label{app:battery}
% TODO: the complete list of 24 battery items and the full per-run golden.json metrics
% (SS + TPI t0/t10 aggregates, sectoral Y_m, residuals).
